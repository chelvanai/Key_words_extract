# BERT-Keywords extractor

BERT-Keywords is a python library to extract important keywords from text using the pretrained BERT model.

It is built on the popular Huggingface Transformers library.

The library makes use of the BERT model to generate an embedding vector for each word in the input text. 

The most important words are then selected using a scoring method based on the word embeddings.
